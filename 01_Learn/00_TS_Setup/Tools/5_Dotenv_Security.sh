# Dotenv (The "Secret Keeper")
# You should never hardcode your MariaDB password in your code. dotenv lets you load variables from a .env file.

# Install: 
npm install dotenv

# Types: 
npm install -D @types/dotenv # (Though modern versions often include their own types).
