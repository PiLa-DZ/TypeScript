# // ESLint checks if the code quality is up to standard.

# Why use it? It catches common Node.js bugs, like forgetting to await a database query.
# Install: 
npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin
npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin --verbose


JSON
"scripts": {
  "lint": "eslint . --ext .ts",
}











